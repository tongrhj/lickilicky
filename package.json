{
  "name": "lickilicky",
  "version": "1.0.0",
  "description": "track f&b partners over time",
  "main": "index.js",
  "author": "Jared Tong <tongrhj@users.noreply.github.com>",
  "license": "MIT",
  "engines": {
    "node": ">=8.8.1"
  },
  "scripts": {
    "build": "mkdir -p dist/data && node ./scripts/firstTimeParseVenue.js && cp -R assets dist/ && cp index.html dist/ && cp now.json dist/ && node ./scripts/updateLastUpdatedDate.js",
    "test": "echo \"Error: no test specified.\""
  },
  "devDependencies": {
    "got": "^9.2.2",
    "husky": "^1.2.1",
    "now": "^13.1.3",
    "prettier": "^1.15.3",
    "pretty-quick": "^1.8.0",
    "query-string": "^6.2.0",
    "replace-in-file": "^3.4.0"
  },
  "husky": {
    "hooks": {
      "pre-commit": "pretty-quick --staged"
    }
  }
}
